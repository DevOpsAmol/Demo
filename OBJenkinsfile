@Library( 'jenkins-pipeline-library' ) _

node {
	def env.dockerHome = tool 'CICDdocker'
	def env.mavenHome = tool 'maven'
	dev env.PATH = "$dockerHome/bin:$mavenHome/bin:$PATH"
	deliveryPipeline('') {
	scmProject = 'DevOpsAmol'
        scmRepository = 'Demo'
        pomFilePath = 'pom.xml'
        buildCommand = 'mvn clean package'
        
    //publishTestReports = [
	//				    reportDir: 'target/jbehave/view',
	//					reportFiles: 'index.html',
	//                  reportName: 'JBehave INT tests report'
	//  ]

		
	}
}
